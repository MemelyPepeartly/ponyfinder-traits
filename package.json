{
  "name": "ponyfinder-traits",
  "version": "1.3.1",
  "description": "Ponyfinder PF2e Traits module",
  "private": true,
  "engines": {
    "node": ">=20.15.0"
  },
  "scripts": {
    "build": "node ./tools/build-local.mjs",
    "deploy:local": "node ./tools/deploy-local.mjs",
    "test:local": "npm run build && npm run deploy:local"
  }
}
